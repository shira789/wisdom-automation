{
  "name": "wisdom-automation",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report",
    "sync": "node scripts/unittcms-sync.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}